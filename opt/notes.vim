" Adding a ! at the end of any settings will toggle on each sourcing of the vimrc file
"
" !silent is used to suppress error messages if the config line
" references plugins/colorschemes that might be missing
